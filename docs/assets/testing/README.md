# Testing evidence

Commands and last recorded counts live in the root README (§27) and CI (`.github/workflows/ci.yml`).

Optional: drop a terminal PNG of `434 passed` / `25 passed` here after a fresh run. Redact secrets.

**CAPTURE REQUIRED** if you want a picture. The pytest/vitest runs themselves are the proof.
