# 49 — AI Optimization Platform

Tune existing paths. Do not change architecture.

Voice stays off the orchestrator wire. Bundle: keep `/` light. Cache: process-local until Redis. GPU: none — Murf/Gemini/LiveKit remain providers.
