# 36 — Desktop Platform

Windows / macOS / Linux clients. No Electron. No installer. No native window UI.

`DesktopService` — multi-window spec, file import/export events, workspace restore, updater check, incremental sync (`desktop_sync`).

Same SDK module list as mobile.
