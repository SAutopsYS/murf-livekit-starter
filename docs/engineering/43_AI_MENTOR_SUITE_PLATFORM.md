# 43 — AI Mentor Suite

Every mentor is an Agent Runtime consumer. `SpecialistRouter` still routes.

Live today: tutor + math guest. Coding/career/interview/language/writing/research are registered-disabled until a specialist ships.

`MentorService.recommend` calls `AgentRuntimeService.execution` — no second mouth.
