# 46 — AI Infrastructure Platform

Hyperscale adapters, not installs.

Redis, Kafka, Kubernetes, Elastic, object storage, vector DB, GPU — all `implemented: false`.

Queues map to existing `JOB_CATALOG`. When a broker lands, jobs do not change names.
