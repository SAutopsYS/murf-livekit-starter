# 35 — Mobile Platform

Another SALORA client. Same SDK modules. No React Native. No Flutter. No mobile UI.

`MobileService` — offline, background sync (`mobile_sync` job), push (architected), deep links (spec), device register.

Consumes Voice, Studio, Learning, Whiteboard, Knowledge through existing contracts.
