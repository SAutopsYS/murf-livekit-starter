# 50 — SALORA Ecosystem Platform

Community + enterprise plugins **are** the marketplace catalog. Templates are Studio `TemplateService`.

`EcosystemService.verify` checks the signed flag. `execute` stays false.
