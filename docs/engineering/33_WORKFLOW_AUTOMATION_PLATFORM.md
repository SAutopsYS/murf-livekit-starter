# 33 — Workflow Automation Platform

Internal automation. Not Zapier. Not n8n. No visual editor.

`AutomationService` is the engine. Studio `WorkflowService.start` is the existing action path. Job: `workflow_run`.

---

## Nodes

trigger, condition, AI/learning/knowledge/studio/whiteboard/marketplace actions, notification, approval, delay, webhook, branch, loop, schedule.

---

## Triggers

VoiceCompleted, LearningFinished, DocumentCreated, WorkflowCompleted, AgentFinished, PluginInstalled, ScheduleTriggered, WebhookReceived, APICalled, OrganizationEvent.

---

## Phase 26

`WorkflowAutomationService = AutomationService`. One execution path.
