# Root compose profile. Service secrets stay in frontend/.env.local and backend/.env.local.
# Copy those from frontend/.env.example and backend/.env.example.

SALORA_PROFILE=development
AUTH_REQUIRED=false
